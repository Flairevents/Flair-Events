.uploadProspectPhoto
  %h1= "Upload Photo for #{@prospect.name}"
  = form_tag "/office/upload_prospect_photo/#{@prospect.id}", multipart: true do
    %p
      %b Photo
      = file_field_tag :photo
    %p= submit_tag 'Upload', class: 'btn btn-default'